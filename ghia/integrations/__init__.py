"""External-service integration modules.

Each submodule wraps one third-party API (GitHub, Docker, etc.) behind
a small, typed surface so the rest of the codebase never sees the
upstream library's types or exceptions directly.
"""
