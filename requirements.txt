# Runtime dependencies for github-issue-agent.
# Pinned to minor; latest-stable as of 2026-04.
fastmcp>=2.3,<3.0
docker>=7.1,<8.0
rich>=13.9,<14.0
pydantic>=2.9,<3.0
starlette>=0.41,<0.46
uvicorn>=0.30,<0.36

# Dev / test dependencies
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<0.26
pytest-cov>=5.0,<7.0
# httpx is needed by tests/test_ui_server.py for ASGI testing.  At
# runtime the agent has no direct httpx dependency (gh CLI is the
# network boundary); fastmcp pulls httpx in transitively for its own
# transport, so this is effectively a "test pin" of an existing dep.
httpx>=0.27,<0.29
# tomli backport for Python 3.10 (stdlib tomllib is 3.11+)
tomli>=2.0,<3.0; python_version < "3.11"
